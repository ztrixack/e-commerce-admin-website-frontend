/*
 * SightBlock Messages
 *
 * This contains all the text for the SightBlock component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SightBlock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SightBlock component!',
  },
});
