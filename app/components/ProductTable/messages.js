/*
 * ProductTable Messages
 *
 * This contains all the text for the ProductTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProductTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductTable component!',
  },
});
