/*
 * EditableRow Messages
 *
 * This contains all the text for the EditableRow component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditableRow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditableRow component!',
  },
});
