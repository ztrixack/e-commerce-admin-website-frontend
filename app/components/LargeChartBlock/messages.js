/*
 * LargeChartBlock Messages
 *
 * This contains all the text for the LargeChartBlock component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LargeChartBlock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LargeChartBlock component!',
  },
});
