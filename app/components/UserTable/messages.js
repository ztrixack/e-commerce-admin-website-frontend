/*
 * UserTable Messages
 *
 * This contains all the text for the UserTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UserTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserTable component!',
  },
});
