/*
 * EditableContext Messages
 *
 * This contains all the text for the EditableContext component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditableContext';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditableContext component!',
  },
});
