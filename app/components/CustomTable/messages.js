/*
 * EditableTable Messages
 *
 * This contains all the text for the EditableTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditableTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditableTable component!',
  },
});
