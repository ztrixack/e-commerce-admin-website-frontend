// import { selectUserEditorPageDomain } from '../selectors';

describe('selectUserEditorPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
