/*
 * AdminLayout Messages
 *
 * This contains all the text for the AdminLayout container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminLayout';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminLayout container!',
  },
});
