// import { selectUserViewerPageDomain } from '../selectors';

describe('selectUserViewerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
