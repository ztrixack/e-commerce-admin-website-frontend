/*
 * UserViewerPage Messages
 *
 * This contains all the text for the UserViewerPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserViewerPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserViewerPage container!',
  },
});
