/*
 * UserProvider Messages
 *
 * This contains all the text for the UserProvider container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserProvider';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserProvider container!',
  },
});
