// import { selectUserProviderDomain } from '../selectors';

describe('selectUserProviderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
