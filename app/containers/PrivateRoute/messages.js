/*
 * PrivateRoute Messages
 *
 * This contains all the text for the PrivateRoute container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PrivateRoute';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PrivateRoute container!',
  },
});
