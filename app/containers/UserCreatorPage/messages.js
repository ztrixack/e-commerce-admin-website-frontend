/*
 * UserCreatorPage Messages
 *
 * This contains all the text for the UserCreatorPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserCreatorPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserCreatorPage container!',
  },
});
