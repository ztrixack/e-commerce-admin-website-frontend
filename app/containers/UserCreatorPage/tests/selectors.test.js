// import { selectUserCreatorPageDomain } from '../selectors';

describe('selectUserCreatorPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
